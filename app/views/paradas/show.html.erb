<div class="page-header">
  <h1>
    <%= @parada.descricao %>
  </h1>
  <div class="pull-right">
    <%= link_to "Editar", edit_parada_path(@parada), class: 'btn btn-primary' %>
  </div>
</div>

<div class="panel panel-default">
  <table class="table">
    <tr>
      <td class="col-md-2">Descrição</td> <td><%= @parada.descricao %></td>
    </tr>
    <tr>
      <td>Data do cadastro</td> <td><%= l @parada.created_at %></td>
    </tr>
  </table>
</div>
