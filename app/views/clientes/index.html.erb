<div class="page-header">
  <h1>Clientes</h1>
  <%= link_to "Novo Cliente", new_cliente_path, class: 'btn btn-primary' %>
</div>

<div class="panel panel-default">
  <table class="table table-striped">
    <thead>
      <tr>
        <th>Código</th>
        <th>Nome</th>
        <th>Telefone</th>
        <th>Email</th>
        <th>Data cadastro</th>
      </tr>
    </thead>

    <tbody>
      <% @clientes.each do |cliente| %>
        <tr>
          <td><%= cliente.codigo %></td>
          <td><%= link_to cliente.nome, cliente %></td>
          <td><%= cliente.telefone %></td>
          <td><%= cliente.email %></td>
          <td><%= l cliente.created_at, format: :xshort %></td>
          <td><%= link_to 'Editar', edit_cliente_path(cliente), class: 'btn btn-default btn-xs' %></td>
        </tr>
      <% end %>
    </tbody>
  </table>
</div>
