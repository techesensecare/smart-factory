<div class="page-header">
  <h1>
    <%= @movimento.descricao %>
  </h1>
  <div class="pull-right">
    <%= link_to "Editar", edit_movimento_path(@movimento), class: 'btn btn-primary' %>
  </div>
</div>

<div class="panel panel-default">
  <table class="table">
    <tr>
      <td class="col-md-2">Descrição</td> <td><%= @movimento.descricao %></td>
    </tr>
    <tr>
      <td>Data do cadastro</td> <td><%= l @movimento.created_at %></td>
    </tr>
  </table>
</div>
