<div class="page-header">
  <h1>
    <%= @celula.codigo %> - <%= @celula.descricao %>
  </h1>
  <div class="pull-right">
    <%= link_to "Editar", edit_celula_path(@celula), class: 'btn btn-primary' %>
  </div>
</div>

<div class="panel panel-default">
  <table class="table">
    <tr>
      <td class="col-md-2">Descrição</td> <td><%= @celula.descricao %></td>
    </tr>
    <tr>
      <td>Data do cadastro</td> <td><%= l @celula.created_at %></td>
    </tr>
  </table>
</div>
