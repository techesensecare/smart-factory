<div class="page-header">
  <h1>
    <%= @contato.descricao %>
  </h1>
  <div class="pull-right">
    <%= link_to "Editar", edit_contato_path(@contato), class: 'btn btn-primary' %>
  </div>
</div>

<div class="panel panel-default">
  <table class="table">
    <tr>
      <td class="col-md-2">Descrição</td> <td><%= @contato.descricao %></td>
    </tr>
    <tr>
      <td>Data do cadastro</td> <td><%= l @contato.created_at %></td>
    </tr>
  </table>
</div>
