<div class="page-header">
  <h1>
    <%= @turno.descricao %>
  </h1>
  <div class="pull-right">
    <%= link_to "Editar", edit_turno_path(@turno), class: 'btn btn-primary' %>
  </div>
</div>

<div class="panel panel-default">
  <table class="table">
    <tr>
      <td class="col-md-2">Descrição</td> <td><%= @turno.descricao %></td>
    </tr>
    <tr>
      <td>Data do cadastro</td> <td><%= l @turno.created_at %></td>
    </tr>
  </table>
</div>
